apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: application
spec:
  replicas: 5
  selector:
    matchLabels:
      app: myApp
  template:
    metadata:
      labels:
        app: myApp
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 8084